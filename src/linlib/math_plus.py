from math import log, e, sin, cos, tan

def ln(num) -> float:
    return log(e, num)

def csc(num) -> float:
    return 1 / sin(num)

def sec(num) -> float:
    return 1 / cos(num)

def cot(num) -> float:
    return 1 / tan(num)
